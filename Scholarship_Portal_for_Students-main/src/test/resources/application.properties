spring.datasource.url=jdbc:mysql://localhost:3306/scholarship_portal_test?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=testpassword
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Provide properties for testing (we can skip sending real emails)
app.email.enabled=false

# Use embedded Redis/Kafka from LocalInfraBootstrap
spring.data.redis.host=localhost
spring.kafka.bootstrap-servers=localhost:9092
